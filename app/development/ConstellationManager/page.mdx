import { Callout } from 'nextra/components'
 
# ConstellationManager

<Callout type="warning">
    ConstellationManager, and its accompanying documentation are still in beta
</Callout>

*Introduction & Overview*

**ConstellationManager** is a standalone application to managing Constellation networks. It uses [UIConstellationManager](/development/modules/UIConstellationManager).
